import React from "react";
import styled from "@emotion/styled";

const City = () => {
  const City = styled.div`
    font-family: "Merriweather Sans", sans-serif;
    font-size: 1.6rem;
  `;
  return <City>Sydney</City>;
};

export default City;
